---Brief News---

делаю т.з. в первый раз, поэтому не на английском языке

--Main:
Создать веб-приложение новостей в кратком изложении со ссылками на источники.

--Server
OS: Ubuntu 22.04.2 LTS
Web server: Apache/2.4.52
Programming language: PHP 8.1.2-1
Backend framework: Laravel 10.13.0
Backend framework libraries: UI, doctrine/dbal
Frontend framework: отсутствует

--Database:
СУБД: MySQL 8.0.33-0
Имя базы данных: brief_news
Пользователь: brief_news_user
Пароль: 'стандартный' (не отображен, т.к. т.з. будет доступено на GitHub)
Дополнительно: Soft delete для пользователей, статей и ссылок; создать первого root (admin привилегии) пользователя


--Роли и статусы пользователей, разграничение прав:
root: first Admin, не подлежит смене роли, не подлежит смене статуса, не подлежит удалению;
Admin: CRUD учетных записей (так-же смена роли пользователей Admin/Writer/Reader, смена статуса пользователей active/ban, восстановление удаленных пользователей), CRUD статей (так-же восстановление удаленных статей), интерфейс Reader, не подлежит смене статуса;
Writer: RU своей учетной записи (не имеет права смены роли и статуса), CRU своих статей, интерфейс Reader;
Reader: RU своей учетной записи, выбирать категории статей по умолчанию (в Мой профиль), сортировка статей по категориям (на Главной), читать статьи;
Unregistered/Unauthenticated: создание учетной записи Reader роли, сортировка статей по категориям (на Главной), читать статьи;

bun статус: не может создавать и редактировать статьи, оставлять коментарии (при реализации их на сайте);

--Дополнительный функционал (возможно будет реализован):
Поиск статьи по заголовку
Поиск статей по created_at

--Interface:
-Header - присутствует на всех страницах:
Узкая горизонтальная полоса на всю ширину экрана с названием сайта (HTML+CSS или картинка или анимация).
Ниже кнопки пользовательского интерфейса расположенные в строку с права на лево, для разных ролей разные:
Admin: Выход, Мой профиль, Все профили, Удаленные статьи, Мои статьи, Создать статью, Главная;
Writer: Выход, Мой профиль, Мои статьи, Создать статью, Главная;
Reader: Выход, Мой профиль, Главная;
Unregistered/Unauthenticated: Регистрация, Вход, Главная.

-Footer: Отсутствует на всех страницах :)

-Страница Главная:
Панель сортировки статей по рубрикам и локали:
Горизонтальная полоса из кнопок: all, policy, economy, science, technologies, sport, other, world, local;
Если нажата all - остальные отжаты;
Если нажаты или отжаты все кроме all - all переходит в нажатое состояние, остальные в отжатое;
All отжата при любом сочетании остальных кнопок;
При выборе одной или нескольких рубрик, локали (world и local) по умолчанию нажаты.

Новости:
Вертикальная лента горизонтальных прямоугольников - заголовков новостей, по 15 на странице.
Сортируются по последней дате. Отображаются статьи выбранных рубрик и локалей.
При нажатии на заголовок раскрывается body статьи, в конце которой ссылки на источники и кнопка свернуть.
При повторном нажатии на заголовок статьи body скрывается.
(возможно будет реализован) кнопку "свернуть" было-бы неплохо расположить справа от текста статьи и приклееть к низу экрана, что бы длинную статью при скролле можно было свернуть из любого места.
У автора статьи (Writer) под заголовком его статей отображается кнопка Редактировать.
У Admin под каждой статьей отображаются кнопки Редактировать, Удалить/Восстановить.

Пагинатор:
Стандартный. Номера страниц со стрелками следующая/предыдущая, в начало/конец.

-Страница Мои статьи (Admin/Writer):
Идентична Главной, отображаются статьи только текущего пользователя.

-Страница Удаленные статьи (Admin):
Идентична Главной, отображаются все удаленные статьи.

-Страница Регистрации (Unregistered/Unauthenticated):
Стандартная страница Laravel/UI библиотеки.
При регистрации обязательные для заполнения поля:
	name (требования библиотеки, можно убрать)
	email
	password
При регистрации назначаемые по умолчанию поля:
	name: NULL
	surname: NULL
	nickname: incognito
	role: reader
	status: active
	rubrick_id: 1
кнопки: Зарегистроваться (Главная), Отмена (Главная).

-Страница Мой профиль (Admin/Writer/Reader):
В верхней части страницы кастомизатор Рубрик отображаемых по умолчанию пользователю на Главной. Горизонтально расположеные названия рубрик с галочками (для наглядного отличия от рубрик на Главной странице). Сочетание галочек подчиняются тем же правилам, что и кнопки рубрик на Главной странице. Изначально пользователю отображаются все рубрики (All, rubrick_id: 1). Валидатор требует поставить хотя бы одну галочку в рубриках и хотя бы одну галочку в локали.

В средней части страницы HTML form с вертикально расположенными предзаполненными полями:
	email
	name
	surname
	nickname

В нижней части страницы кнопки Изменить пароль, Сохранить (Главная), Отмена (Главная).

-Страница Изменить пароль (Admin/Writer/Reader):
Вертикально расположенные поля:
	Введите текущий пароль
	Введите новый пароль
	Введите новый пароль еще раз
В нижней части страницы кнопки Сохранить новый пароль (Мой профиль), Отмена (Мой профиль).

-Страница Все профили (Admin):
Панель сортировки пользователей по роли и статусу (будет реализовыватся после запуска сайта как фича):
Горизонтальная полоса из кнопок: All, Admin, Writer, Reader, Bunned, Deleted.
Если нажата all - остальные отжаты;
Если нажаты или отжаты все кроме all - all переходит в нажатое состояние, остальные в отжатое;
All отжата при любом сочетании остальных кнопок;

Найти профиль пользователя (будет реализовыватся после запуска сайта как фича):
Прямоугольная область HTML form с полями: id, name, surname, nickname, email, created_at, deleted_at.
В нижней правой части области кнопки: Поиск, Очистить все.

Профили пользователей:
Вертикальная лента горизонтальных прямоугольников - пользователей, по 15 на странице.
Сортируются по последнему id. Отображаются пользователи выбранных ролей и статусов.
Отображаемые поля пользователя: id, name, surname, nickname, role, status, email, rubrick_id, created_at, deleted_at.
Под пользователем кнопка "Редактировать профиль пользователя Name".
Удаленные пользователи имеют другое оформление (будет реализовыватся после запуска сайта как фича).

-Страница Редактировать профиль пользователя Name (Admin):
HTML form с вертикально расположенными предзаполненными полями:
	id
	name
	surname
	nickname
	role
	status
	rubrick_id
	email
	email_verified_at
	галочка "Изменить пароль", установить для изменения пароля и тогда password должен быть заполнен
	password - пустое поле с фоновой надписью "Will not change (Не изменится)"
	created_at
	updated_at
	deleted_at
В нижней части страницы кнопки Сохранить изменения (Все профили), Отмена (Все профили).

-Страница Создать и Редактировать статью (Admin/Writer):
Рубрика и локаль статьи:
Горизонтально расположеные названия с галочками: policy, economy, science, technologies, sport, other, world, local. Изначально все галочки пусты, валидатор требует поставить хотя бы одну.

Статья:
HTML form с вертикально расположенными полями:
	header
	body
	sources links - изначально одно пустое поле, после начала ввода появляется поле для следующей ссылки

В нижней части страницы кнопки Сохранить (Главная), Отмена (Главная).

-Страница Полного Редактирования статьи (Admin):
Информация об Writer:
	id
	name
	surname
	nickname
	role
	status
	email
	created_at
	deleted_at
Если текущий пользователь (Admin) автор статьи, то вместо полной информации написанно "Моя статья".

Рубрики и локали:
Горизонтально расположеные названия рубрик с галочками установленными при создании статьи: policy, economy, science, technologies, sport, other, world, local. Валидатор требует поставить хотя бы одну.

Статья:
HTML form с вертикально расположенными полями:
	id (не изменяется)
	header
	body
	sources links
	created_at
	deleted_at

В нижней части страницы кнопки Сохранить (Главная), Отмена (Главная).